{
  "id": "env_optimizer_ai_v1",
  "name": "env_optimizer_ai_v1",
  "description": "AI auto tuning for the trading system with safe env generation and apply flow",
  "version": "1.0.0",
  "enabled": true,
  "category": "optimizer",
  "price": 0,
  "author": "system",
  "tools": [
    {
      "name": "auto_optimize_env",
      "description": "Generate the best env candidate from recent trades",
      "input_schema": {
        "type": "object",
        "properties": {
          "sample_size": { "type": "integer" },
          "num_candidates": { "type": "integer" }
        }
      }
    },
    {
      "name": "apply_best_env",
      "description": "Read latest.env and return apply result",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
