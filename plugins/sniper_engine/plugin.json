{
  "id": "sniper_engine",
  "name": "Sniper Engine PRO+",
  "description": "Early token sniper using pump scan, wallet alpha, rug guard and real execution with Jito acceleration",
  "version": "2.0.0",
  "enabled": true,
  "category": "execution",
  "price": 0,
  "author": "system",
  "tools": [
    {
      "name": "sniper_scan",
      "description": "Scan early tokens from pump and filter by wallet alpha and rug guard",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "start_sniper",
      "description": "Start sniper loop",
      "input_schema": {
        "type": "object",
        "properties": {
          "capital": {
            "type": "number",
            "description": "Capital used by sniper"
          }
        }
      }
    },
    {
      "name": "stop_sniper",
      "description": "Stop sniper loop",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
