{
  "id": "wallet_alpha_v2",
  "name": "wallet_alpha_v2",
  "description": "Smart money alpha with scoring, decay and clustering",
  "version": "2.0.0",
  "enabled": true,
  "category": "alpha",
  "price": 0,
  "author": "system",
  "tools": [
    {
      "name": "wa_record_trade",
      "description": "record wallet trade",
      "input_schema": {
        "type": "object",
        "properties": {
          "wallet": { "type": "string" },
          "asset_id": { "type": "string" },
          "side": { "type": "string" },
          "price": { "type": "number" },
          "size": { "type": "number" },
          "timestamp": { "type": "number" }
        },
        "required": ["wallet","asset_id","side","price","size"]
      }
    },
    {
      "name": "get_wallet_alpha_v2",
      "description": "return wallet alpha signal",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_id": { "type": "string" }
        },
        "required": ["asset_id"]
      }
    },
    {
      "name": "wa_get_top_wallets",
      "description": "get best wallets",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
