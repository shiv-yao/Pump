{
  "id": "replay_engine_v1",
  "name": "replay_engine_v1",
  "description": "Trade-level replay engine integrated with execution simulator",
  "version": "1.1.0",
  "enabled": true,
  "category": "backtest",
  "price": 0,
  "author": "system",
  "tools": [
    {
      "name": "replay_run",
      "description": "Run replay with one config",
      "input_schema": {
        "type": "object",
        "properties": {
          "config": { "type": "object" },
          "sample_size": { "type": "integer" }
        }
      }
    },
    {
      "name": "replay_optimize",
      "description": "Find best config using replay",
      "input_schema": {
        "type": "object",
        "properties": {
          "sample_size": { "type": "integer" },
          "num_candidates": { "type": "integer" }
        }
      }
    }
  ]
}
