{
  "id": "wallet_alpha_v3",
  "name": "wallet_alpha_v3",
  "description": "Smart money alpha v3 with clustering + leader detection",
  "version": "3.0.0",
  "enabled": true,
  "category": "alpha",
  "price": 0,
  "tools": [
    {
      "name": "wa_record_trade",
      "description": "record wallet trade",
      "input_schema": {
        "type": "object",
        "properties": {
          "wallet": {"type": "string"},
          "asset_id": {"type": "string"},
          "side": {"type": "string"},
          "price": {"type": "number"},
          "size": {"type": "number"},
          "timestamp": {"type": "number"}
        },
        "required": ["wallet","asset_id","side","price","size"]
      }
    },
    {
      "name": "get_wallet_alpha_v3",
      "description": "smart money alpha",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_id": {"type": "string"}
        },
        "required": ["asset_id"]
      }
    },
    {
      "name": "wa_get_leaders",
      "description": "top leader wallets",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
