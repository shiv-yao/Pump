{
  "id": "env_optimizer_ai_v2",
  "name": "env_optimizer_ai_v2",
  "description": "AI auto tuning with replay engine integration and safe fallback",
  "version": "2.0.0",
  "enabled": true,
  "category": "optimizer",
  "price": 0,
  "author": "system",
  "tools": [
    {
      "name": "auto_optimize_env",
      "description": "Optimize env config using replay first, then fallback search",
      "input_schema": {
        "type": "object",
        "properties": {
          "sample_size": { "type": "integer" },
          "num_candidates": { "type": "integer" }
        }
      }
    },
    {
      "name": "apply_best_env",
      "description": "Load and parse latest.env",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
