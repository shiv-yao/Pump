async def handle_mempool(event):
    if event["type"] == "swap":
        # 👉 這裡可以升級成 decode 真 token
        engine.log("MEMPOOL HIT")

        # 先隨機進（可改成解析 mint）
        mint = TEST_TARGET_MINT

        if mint:
            await buy(mint)
